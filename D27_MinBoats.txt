//Leetcode Ques Link: https://leetcode.com/problems/boats-to-save-people/

int numRescueBoats(vector<int>& people, int limit) {
    int n=people.size();
    sort(people.begin(),people.end());
    int i=0,j=n-1,count=0;
    while(i<=j){
        if((people[i]+people[j]) <= limit){ 
            i++;
            j--;
        }
        else j--;
        count++;
    }
    return count;
}